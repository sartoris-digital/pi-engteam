---
name: factory-jira
description: Jira tracker candidate for pi-sdlc-factory. No adapter in v1; doctor lists this skill as a candidate only.
metadata:
  pi-sdlc-factory-tracker: jira
  pi-sdlc-factory-adapter: cli:jira
---

# factory-jira

Jira tracking is planned for v2 (`jira-cli`). This skill is documentation for models and a doctor candidate. It does not enable an adapter.

Do not call `jira` from a worker. Do not invent a v1 Jira adapter.
