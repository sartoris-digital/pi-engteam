import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Controller-session registration. v0 stub: Task 9.x wires config, engine, executor, lanes,
 * workspace, commands and session_start recovery behind this exact signature.
 */
export function registerController(_pi: ExtensionAPI): void {
  // intentionally empty in v0 until the controller tasks land
}
