/**
 * Re-export of the one generated-artifact marker definition (src/home.ts, Task 0.2).
 * Runtime, worker, engine, gate and steer modules all import it from here or from ../home.js;
 * the contract line is never retyped, so changing it is a one-file edit.
 */
export { generatedMarker, GENERATED_MARKER_RE } from "../home.js";
