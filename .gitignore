node_modules/
dist/*
# Pre-built server bundle ships with the repo so postinstall works without devDependencies
!dist/server.cjs
.worktrees/

# Local agent / tooling state — never commit (may contain secrets, e.g. .pi/ vault)
.pi/
.omc/
.omx/
.serena/
.claude/
fix-plan.md
